"""Ваши ответы по фрагментам из fragments.py · занятие 3.

Заполните оба словаря и запустите проверку:

    pytest course-materials/complexity/test_lesson_03.py
или 
    python -m pytest test_lesson_03.py -q

Проверка сравнивает не текст, а отпечаток ответа, поэтому подсмотреть
правильные оценки в тестах нельзя. Зато можно проверять себя сколько угодно
раз: тест называет номер фрагмента, но не называет верный ответ.

Как записывать оценку:

    O(1)        O(log n)      O(n)        O(n log n)      O(n^2)
    O(n + m)    — когда входов два и они растут независимо

Регистр букв и пробелы значения не имеют: «o(N LOG N)» засчитается.
Записи «O(2n)», «O(n/2)» и «O(n^2 + n)» — нет: константы и младшие
слагаемые в оценке не пишутся.

Незаполненный ответ — пустая строка. Тест покажет его как «не заполнено»,
а не как ошибку.
"""

# Время: как растёт число операций с ростом входа.
TIME = {
    "01_find_pass": "O(n)",
    "02_first_and_last": "O(1)",
    "03_has_duplicate_badges": "O(n^2)",
    "04_has_duplicate_badges_fast": "O(n)", # будет O(n), так как один цикл for
    "05_three_cheapest": " ", 
    "06_find_receipt": "O(log n)", #так как при бинарном поиске log n мы отбрасываем половину элементов
    "07_average_and_spikes": "O(n)", # два цикла идут друг за другом. итого O(2n), и двойку убираем
    "08_closest_pair_distance": "O(n^2) ", # два цикла вложенных
    "09_word_counts": "O(n)", 
    "10_unique_keep_order": "O(n^2)", # это именно O(n^2) так как цикл for O(n), а внутри него if row not in result и result это список
    "11_halving_steps": "O(log n)", # мы делим число пополам
    "12_merge_sorted": " ",
}

# Память: сколько функция занимает дополнительно к тому, что ей передали.
SPACE = {
    "01_find_pass": "O(1)",
    "02_first_and_last": "O(1)",
    "03_has_duplicate_badges": "O(1)", # только переменные i и j, память доп. не используется
    "04_has_duplicate_badges_fast": "O(n)", # у нас именно seen = set() создаётся, поэтому память растет как O(n)
    "05_three_cheapest": " ",
    "06_find_receipt": "O(1)", # ничего не создаём
    "07_average_and_spikes": "O(1)",
    "08_closest_pair_distance": "O(1)",
    "09_word_counts": "O(n)",  
    "10_unique_keep_order": "O(n)",   
    "11_halving_steps": "O(1)", # создали счётчик steps
    "12_merge_sorted": " ",
}

# Худший случай двух фрагментов — одной строкой: при каком входе
# работы больше всего и почему.
WORST_CASE = {
    "01_find_pass": "нужноо номера нету в списке",
    "04_has_duplicate_badges_fast": "в списке нет вообще дупликатов",
}
